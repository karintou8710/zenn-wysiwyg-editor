{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "lib/**"]
    },
    "lint": {
      "outputs": []
    },
    "fix": {
      "outputs": ["src/**"]
    }
  }
}
